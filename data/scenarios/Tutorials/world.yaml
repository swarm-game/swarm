name: Foobar
description: |
  Learn how to do stuff
objectives:
  - goal:
      - Your first goal is to get three trees.
    condition: |
      try {
        n <- as base {count "tree"};
        return (n >= 3)
      } { return false }
  - goal:
      - Nice job.  Now, build a harvester.
    condition: |
      try { has "harvester" } {return false}
robots:
  - name: base
    display:
      char: 'Ω'
      attr: robot
    loc: [0,0]
    dir: [0,1]
    devices:
      - 3D printer
      - dictionary
      - grabber
      - life support system
      - logger
      - toolkit
      - solar panel
      - workbench
      - clock
    inventory:
      - [5, 3D printer]
      - [100, treads]
      - [70, grabber]
      - [100, solar panel]
      - [50, scanner]
      - [5, toolkit]
world:
  seed: 0
  offset: true