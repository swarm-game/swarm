version: 1
name: Test world DSL
description: foo
robots:
  - name: base
    loc: [0,0]
    dir: [1,0]
    devices:
      - 3D printer
      - logger
      - treads
      - grabber
known: [tree]
world:
  dsl: |
    overlay
      [ if (x + y >= 2) then {stone, tree} else {dirt}
      , if (x < (-5)) then ({stone} <> {stone, flower}) else {dirt}
      ]
