format (0
    + 1
    * 2
    - 3
    / 4
    // This is a comment, not division
    /* This is also a comment */
    ^ 5
)

++ format (true
    || true
    && true
)

++ format (
    (1 <= 2) ||
    (1 >= 2) ||
    (1 > 2) ||
    (1 < 2) ||
    (1 == 2) ||
    (1 != 2)
)

++ format (
    scan $ north
)

++ format (
    1 : Int
)