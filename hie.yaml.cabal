-- If you want to use HLS with Cabal (recommended), simply do
--
--   cp hie.yaml.cabal hie.yaml
--
-- the file hie.yaml is ignored, so it will not
-- show in git status ;)
--
-- If you are using Stack, you can ignore this file.
cradle:
  cabal:
